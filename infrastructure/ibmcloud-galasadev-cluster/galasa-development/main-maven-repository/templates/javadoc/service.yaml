#
# Copyright contributors to the Galasa project 
# 
apiVersion: v1
kind: Service
metadata:
  labels:
    app: javadocs-{{ .Values.branch }}
  name: javadocs-{{ .Values.branch }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: 80
  selector:
    app: javadocs-{{ .Values.branch }}