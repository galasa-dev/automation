#
# Copyright contributors to the Galasa project 
# 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: isolated-{{ .Values.branch }}
  namespace: {{ .Values.namespace }}
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: isolated-{{ .Values.branch }}
  template:
    metadata:
      name: isolated-{{ .Values.branch }}
      labels:
        app: isolated-{{ .Values.branch }}
    spec:
      containers:
        - image: harbor.galasa.dev/galasadev/galasa-isolated:{{ .Values.imageTag }}
          imagePullPolicy: Always
          name: isolated-{{ .Values.branch }}
          env:
          - name: CONTEXTROOT
            value: {{ .Values.branch }}/maven-repo/isolated
          ports:
            - containerPort: 80