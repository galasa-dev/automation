#
# Copyright contributors to the Galasa project 
# 
apiVersion: v1
kind: Service
metadata:
  labels:
    app: extensions-{{ .Values.branch }}
  name: extensions-{{ .Values.branch }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: 80
  selector:
    app: extensions-{{ .Values.branch }}