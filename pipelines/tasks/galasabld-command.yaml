#
# Copyright contributors to the Galasa project
#
# SPDX-License-Identifier: EPL-2.0
#

apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: galasabld
  namespace: galasa-build
spec:
  workspaces:
  - name: git-workspace
    mountPath: /workspace/git
  params:
  - name: context
    type: string
  - name: command
    type: array
  - name: galasabldImageTag
    type: string
    default: main
  - name: image
    type: string
    defualt: harbor.galasa.dev/galasadev/galasabld-amd64:$(params.galasabldImageTag)
  steps:
  - name: galasabld
    workingDir: /workspace/git/$(params.context)
    image: $(params.image)
    imagePullPolicy: Always
    command:
    - galasabld
    - $(params.command[*])
    volumeMounts:
    - name: githubcreds
      mountPath: /creds
    - name: harborcreds
      mountPath: /harborcreds