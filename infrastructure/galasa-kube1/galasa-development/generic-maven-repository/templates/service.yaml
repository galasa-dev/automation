#
# Copyright contributors to the Galasa project
#
# SPDX-License-Identifier: EPL-2.0
#

apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.name }}-{{ .Values.branch }}
  name: {{ .Values.name }}-{{ .Values.branch }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 80
  selector:
    app: {{ .Values.name }}-{{ .Values.branch }}
