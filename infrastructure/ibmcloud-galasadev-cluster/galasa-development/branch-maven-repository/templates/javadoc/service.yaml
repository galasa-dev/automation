#
# Copyright contributors to the Galasa project 
# 
apiVersion: v1
kind: Service
metadata:
  labels:
    app: javadocs-{{ .Values.javadoc.branch }}
  name: javadocs-{{ .Values.javadoc.branch }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: 80
  selector:
    app: javadocs-{{ .Values.javadoc.branch }}