apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tekton-build-workspace-pvc
  namespace: galasa-pipelines
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
