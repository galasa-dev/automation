#
# Copyright contributors to the Galasa project 
# 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eclipse-{{ .Values.branch }}
  namespace: {{ .Values.namespace }}
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: eclipse-{{ .Values.branch }}
  template:
    metadata:
      name: eclipse-{{ .Values.branch }}
      labels:
        app: eclipse-{{ .Values.branch }}
    spec:
      containers:
        - image: harbor.galasa.dev/galasadev/galasa-eclipse:{{ .Values.imageTag }}
          imagePullPolicy: Always
          name: eclipse-{{ .Values.branch }}
          env:
          - name: CONTEXTROOT
            value: {{ .Values.branch }}/maven-repo/eclipse
          ports:
            - containerPort: 80