#
# Copyright contributors to the Galasa project 
# 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wrapping-{{ .Values.branch }}
  namespace: {{ .Values.namespace }}
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: wrapping-{{ .Values.branch }}
  template:
    metadata:
      name: wrapping-{{ .Values.branch }}
      labels:
        app: wrapping-{{ .Values.branch }}
    spec:
      containers:
        - image: harbor.galasa.dev/galasadev/galasa-wrapping:{{ .Values.imageTag }}
          imagePullPolicy: Always
          name: wrapping-{{ .Values.branch }}
          env:
          - name: CONTEXTROOT
            value: {{ .Values.branch }}/maven-repo/wrapping
          ports:
            - containerPort: 80
              name: wrapping-{{ .Values.branch }}