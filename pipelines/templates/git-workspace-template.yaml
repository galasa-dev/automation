# Used for kicking off pipelines using tkn CLI
# A volumeClaimTemplate file must be passed in

spec:
  storageClassName: longhorn-temp
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi