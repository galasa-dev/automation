#
# Copyright contributors to the Galasa project 
# 
apiVersion: v1
kind: Service
metadata:
  labels:
    app: obr
  name: obr
  namespace: galasa-development
spec:
  ports:
    - port: 80
  selector:
    app: obr