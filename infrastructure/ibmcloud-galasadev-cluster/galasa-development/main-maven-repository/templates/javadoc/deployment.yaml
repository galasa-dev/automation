#
# Copyright contributors to the Galasa project 
# 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: javadoc-{{ .Values.branch }}
  namespace: {{ .Values.namespace }}
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: javadoc-{{ .Values.branch }}
  template:
    metadata:
      name: javadoc-{{ .Values.branch }}
      labels:
        app: javadoc-{{ .Values.branch }}
    spec:
      containers:
        - image: harbor.galasa.dev/galasadev/galasa-javadoc-maven-repo:{{ .Values.imageTag }}
          imagePullPolicy: Always
          name: javadoc-{{ .Values.branch }}
          env:
          - name: CONTEXTROOT
            value: {{ .Values.branch }}/maven-repo/javadoc
          ports:
            - containerPort: 80
              name: javadoc-{{ .Values.branch }}