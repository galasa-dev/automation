apiVersion: v1
kind: Service
metadata:
  labels:
    app: gradle-{{ .Values.branch }}
  name: gradle-{{ .Values.branch }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: 80
  selector:
    app: gradle-{{ .Values.branch }}