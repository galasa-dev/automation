#
# Copyright contributors to the Galasa project 
#
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: git-status
  namespace: galasa-pipelines
spec:
  params:
  - name: status
    type: string
  - name: prUrl
    type: string
  - name: statusesUrl
    type: string
  - name: issueUrl
    type: string
  - name: pipelineRunName
    type: string
  steps:
  - name: github-status
    workingDir: /workspace/git/
    image: harbor.galasa.dev/common/ghstatus:latest
    env:
    - name: GITHUBTOKEN
      valueFrom:
        secretKeyRef:
          name: github-token
          key: token
          optional: false
    command:
    - /go/bin/ghstatus
    - --status
    - $(params.status)
    - --prUrl
    - $(params.prUrl)
    - --statusesUrl
    - $(params.statusesUrl)
    - --issueUrl
    - $(params.issueUrl)
    - --pipelineRunName
    - $(params.pipelineRunName)